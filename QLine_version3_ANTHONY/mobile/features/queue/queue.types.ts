export type Eta = { mean: number; low: number; high: number; avgServiceSec: number };
export type QueueItem = { id: string; joinedAt: string };
export type SavedTicket = { commerceId: string; ticketId: string };
